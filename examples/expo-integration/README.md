# Expo Integration Test App

This example app runs a simple integration test against the Expo SQLite adapter
on launch and shows PASS/FAIL in the UI.

## Run

```sh
cd examples/expo-integration
npm install
npx expo start
```

Open the app on a device/simulator. The status text updates once the test
finishes.
